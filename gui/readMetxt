flow 

run combine.py

startpage -> login -> greeting -> questions -> answers -> end

# every file has two button - prev and next
# so import the prev and next class only

# after creating new page add it in combine.py tuple too



todo:
can we have widgets appearing one after another after a set time interval